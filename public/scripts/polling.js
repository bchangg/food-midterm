$(document).ready(function() {
  // console.log('LETS POLL');
  // $pendingItems = $('.status')
  // console.log($pendingItems);
  // $hiddenId = $('.status input').toArray();
  // const statusIdObj = {}

  // for (let i = 0; i < $pendingItems.length; i++) {
  //   if (!statusIdObj[$hiddenId[i].defaultValue]) {
  //     statusIdObj[$hiddenId[i].defaultValue] = $pendingItems[i].innerText;
  //   }
  // }

  // $('.btn').click(function(event) {
  //   $orderStatus = $(this).prev().prev()[0].defaultValue;
  //   $orderId = $(this).prev()[0].defaultValue;
  //   $buttonStatusId = { orderId: $orderId, orderStatus: $orderStatus };

  //   // $.post("/restaurants/cancel", $buttonStatusId, function(request, response) {
  //   //   console.log(request);
  //   // })
  // })

  // restaurantPolling();
  // function restaurantPolling() {
  //   setTimeout(function() {
  //     restaurantPolling();
  //   }, 3000);
  // }
})
