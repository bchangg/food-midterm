<div class="order-details-container">
  <span id="close">X</span>
  <div class="order-details">
    <% if (user) { %>
    <span>Hello <%= user %>, this is your order!</span>
    <% } else {%>
    <span>Hello Guest, this is your order!</span>
    <% } %>
    <div class="order-price-summary d-flex flex-row justify-content-between align-items-center">
      <span id="final-price">test</span>
      <button id="place-order" class="btn btn-primary btn-lg" type="button" name="button">Place Order</button>
    </div>
  </div>
</div>
